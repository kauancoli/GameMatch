export type LoginFormValues = {
  email: string;
  password: string;
};

export type SignScreenProps = {
  setUser: any;
  user: any;
};

export type RegisterFormValues = {
  email: string;
  password: string;
  confirmPassword: string;
};

export type ForgotPasswordFormValues = {
  email: string;
};
